let promoCode = prompt("Введите промокод"); // Запрашиваем промокод у пользователя 
    if(promoCode ==="СкиДка") {
        alert("Промокод применён");
    } else {
        alert("Промокод не работает");
    }
